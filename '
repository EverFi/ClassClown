require 'bundler'
Bundler.setup
require 'sinatra'

class ClassClown < Sinatra::Base

  before do 
    puts settings.inspect
  end

  get "/" do
    erb :index
  end

  get "/record_result" do
    quiz = params[:quiz_id]
    answer = params[:answer_id]
  end
end
